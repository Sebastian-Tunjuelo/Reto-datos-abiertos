# D1.1 — Descarga y limpieza EVA histórica 2007-2018

## Metadatos
| Campo | Valor |
|-------|-------|
| ID | D1.1 |
| Módulo | `modules/agricultural/ingestion.py` → `download_eva_historica()` |
| Depende de | — (sin dependencias) |
| Produce para | D1.3 (unificación) |
| Paralelo con | D1.2 (independiente) |
| Estado | ✅ Completo |

---

## Agente

**Rol**: Eres un ingeniero de datos Python especializado en ingesta de APIs REST y limpieza de datos tabulares. Tu trabajo es implementar una función de descarga robusta que consuma la API Socrata de datos.gov.co y retorne datos limpios y tipados correctamente.

**Mentalidad**: Piensa en términos de contratos de datos. Tu función recibe parámetros y debe retornar un DataFrame con un esquema exacto y predecible. Cualquier dato que no cumpla el esquema debe ser corregido o marcado como nulo — nunca silenciado ni ignorado. Prefiere código explícito sobre código "inteligente".

**Restricciones**:
- No inventar lógica de negocio que no esté en esta spec
- No agregar columnas que no estén en el esquema unificado
- No usar `sodapy` — usar `shared/socrata_client.py` directamente
- No usar `inplace=True` en operaciones Pandas
- No hardcodear los códigos DANE ni los rangos de rendimiento — usar `shared/`
- Si algo no está especificado, preguntar antes de asumir

---

## Objetivo
Descargar los registros de EVA 2007-2018 para los 15 municipios del MVP y los 3 cultivos, limpiarlos y retornar un DataFrame con el esquema unificado del proyecto.

---

## Fuente de datos
| Campo | Valor |
|-------|-------|
| Dataset ID | `2pnw-mmge` |
| URL | `https://www.datos.gov.co/resource/2pnw-mmge.json` |
| Registros totales | ~180.000 (Colombia completa) |
| Registros esperados MVP | ~1.500–2.000 |

### Columnas reales en la API
| Columna API | Tipo | Descripción |
|-------------|------|-------------|
| `c_d_mun` | string | Código DANE (puede venir sin cero inicial, ej. `'5036'`) |
| `municipio` | string | Nombre en MAYÚSCULAS |
| `departamento` | string | Nombre en MAYÚSCULAS |
| `cultivo` | string | `'CAFÉ'`, `'CACAO'`, `'MAÍZ'` |
| `a_o` | string | Año: `'2007'`..`'2018'` |
| `periodo` | string | `'2015A'`, `'2015B'` o `'2015'` |
| `rendimiento_t_ha` | string | t/ha (puede ser vacío o `'0'`) |
| `rea_sembrada_ha` | string | ha |
| `rea_cosechada_ha` | string | ha |
| `producci_n_t` | string | toneladas |
| `ciclo_de_cultivo` | string | `'PERMANENTE'` o `'TRANSITORIO'` |

### Filtro SoQL implementado
```
$where=cultivo IN ('CAFÉ','CACAO','MAÍZ') AND c_d_mun IN (
  '73001','73168','41001','41298','41551','68689','68615',
  '05036','5036','05030','5030',
  '17541','17524','50001','19256','19418','20001'
)
```
> Incluye ambas formas del código DANE (con y sin cero inicial) directamente en el `IN` de SoQL. El paso 4 normaliza a 5 dígitos y el filtro Python actúa como red de seguridad.

---

## Mapeo de columnas → esquema unificado
| Columna API | Columna unificada |
|-------------|-------------------|
| `c_d_mun` | `codigo_dane` |
| `municipio` | `municipio` |
| `departamento` | `departamento` |
| `cultivo` | `cultivo` |
| `a_o` | `año` |
| `periodo` | `periodo` |
| `rendimiento_t_ha` | `rendimiento` |
| `rea_sembrada_ha` | `area_sembrada` |
| `rea_cosechada_ha` | `area_cosechada` |
| `producci_n_t` | `produccion` |
| `ciclo_de_cultivo` | `ciclo` |
| *(agregar)* | `fuente = 'historica'` |

---

## Lógica de transformación
```
1. Llamar fetch_all(dataset_id='2pnw-mmge', where=<filtro SoQL>)
2. Convertir lista de dicts → pd.DataFrame
3. Renombrar columnas según mapeo
4. normalize_dane_code(codigo_dane) → str 5 dígitos
5. Filtrar: conservar solo filas donde codigo_dane esté en codigos_objetivo
   (captura los que llegaron sin cero inicial)
6. normalize_cultivo(cultivo) → 'Café'|'Cacao'|'Maíz'|None
   Descartar filas donde cultivo sea None; loggear cuántas y qué valores
7. normalize_title_case(municipio) y normalize_title_case(departamento)
8. año = pd.to_numeric(año, errors='coerce').astype('Int64')
9. rendimiento = pd.to_numeric(rendimiento, errors='coerce')
   rendimiento donde valor == 0.0 → None
   rendimiento fuera de RENDIMIENTO_RANGOS[cultivo] → None; loggear municipio y cultivo
10. area_sembrada, area_cosechada, produccion = pd.to_numeric(..., errors='coerce')
11. ciclo: conservar 'PERMANENTE'|'TRANSITORIO'; cualquier otro valor → None
12. fuente = 'historica'
13. Eliminar duplicados por (codigo_dane, cultivo, año, periodo, fuente)
    Loggear cuántos duplicados se eliminaron
14. Loggear warning por cada municipio del objetivo sin datos en el resultado
15. Retornar DataFrame con columnas en orden del esquema unificado
```

---

## Notas de implementación

### Tipo de `año`
Se usa `Int64` (nullable integer de pandas) en lugar de `int64`. Razón: si algún registro tiene año nulo o no parseable, `int64` lanzaría una excepción al castear; `Int64` lo convierte a `pd.NA` sin romper el pipeline. En la práctica todos los registros tienen año válido, pero la robustez es preferible.

### Validación de rendimiento por fila
Se usa `df.apply()` en lugar de operaciones vectorizadas para poder loggear el municipio y cultivo exactos de cada valor fuera de rango. El volumen (~1.500–2.000 filas) hace que el costo de `apply` sea despreciable.

### Filtro SoQL doble para códigos DANE
El `_WHERE_HISTORICA` incluye `'05036'` y `'5036'` (y `'05030'`/`'5030'`) en el mismo `IN`. Esto reduce el trabajo en Python porque la API ya filtra ambas formas. El paso 4 normaliza y el filtro Python es la red de seguridad final.

---

## Manejo de errores
| Situación | Comportamiento |
|-----------|---------------|
| Municipio sin registros en la fuente | Warning: `"[D1.1] Sin datos para {municipio} en EVA histórica"` |
| `rendimiento` vacío o `'0'` | → `None` (cero no es válido agronómicamente) |
| `rendimiento` fuera de rango | → `None`; loggear municipio y cultivo afectados |
| Cultivo no reconocido (ej. `'MAIZ AMARILLO'`) | Descartar fila; loggear valor inesperado |
| Error HTTP de Socrata | `fetch_all()` reintenta 3 veces; si falla, propagar excepción |
| DataFrame vacío tras filtros | Lanzar `ValueError("EVA histórica: sin registros para el MVP")` |

---

## Interfaz de la función
```python
def download_eva_historica(
    codigos_dane: list[str] | None = None,
) -> pd.DataFrame:
    """
    Descarga y limpia EVA histórica 2007-2018.

    Args:
        codigos_dane: Lista de códigos DANE a filtrar.
                      Si None, usa MVP_CODIGOS de shared/dane_codes.py.

    Returns:
        DataFrame con esquema unificado del proyecto.
        Columnas: codigo_dane, municipio, departamento, cultivo,
                  año, periodo, rendimiento, area_sembrada,
                  area_cosechada, produccion, ciclo, fuente.

    Raises:
        ValueError: Si el resultado está vacío tras aplicar filtros.
        requests.RequestException: Si la API falla tras reintentos.
    """
```

---

## Criterios de aceptación
- [x] El DataFrame retornado tiene exactamente las 12 columnas del esquema unificado
- [x] `codigo_dane` tiene siempre 5 caracteres en todos los registros
- [x] `cultivo` contiene solo `'Café'`, `'Cacao'` o `'Maíz'`
- [x] `año` es `Int64` (nullable); no hay valores fuera de rango 2007–2018 en datos reales
- [x] `rendimiento` es `float64`; no hay valores `0.0`
- [x] `fuente` es siempre `'historica'`
- [x] No hay duplicados por `(codigo_dane, cultivo, año, periodo, fuente)`
- [x] Se loggea un warning por cada municipio sin datos
- [x] La función acepta `codigos_dane=None` y usa `MVP_CODIGOS` por defecto
