# D1.3 — Unificación, validación y guardado Parquet

## Metadatos
| Campo | Valor |
|-------|-------|
| ID | D1.3 |
| Módulo | `modules/agricultural/ingestion.py` → `run_pipeline()` |
| Depende de | D1.1 (df_historica) + D1.2 (df_reciente) |
| Produce para | D1.4 (validación), D5 (tabla maestra) |
| Estado | 🔲 Pendiente |

---

## Agente

**Rol**: Eres un ingeniero de datos Python responsable de la capa de orquestación del pipeline EVA. Tu trabajo es ensamblar los outputs de D1.1 y D1.2, garantizar la integridad de la unión y persistir los resultados en disco en formato Parquet.

**Mentalidad**: Piensa como el último guardián de calidad antes de que los datos lleguen al modelo. No confíes ciegamente en que D1.1 y D1.2 hicieron su trabajo perfectamente — valida la consistencia entre fuentes antes de guardar. Un error aquí se propaga silenciosamente a todas las capas siguientes (features, modelo, predicciones). Sé explícito en los logs para que cualquier problema sea visible inmediatamente.

**Restricciones**:
- No re-limpiar columnas individuales — eso es responsabilidad de D1.1 y D1.2
- No modificar los DataFrames de entrada antes de guardarlos como `historica` y `reciente`
- No lanzar excepciones por advertencias de consistencia — solo loggear
- Sí lanzar `AssertionError` si la integridad de la unión falla
- El orden de columnas en el Parquet debe seguir el esquema unificado definido en el README

---

## Objetivo
Recibir los dos DataFrames limpios de D1.1 y D1.2, concatenarlos, aplicar validaciones finales de consistencia, guardar los 3 archivos Parquet e imprimir el resumen de ejecución.

---

## Inputs
| Input | Tipo | Origen |
|-------|------|--------|
| `df_historica` | `pd.DataFrame` | Retorno de `download_eva_historica()` (D1.1) |
| `df_reciente` | `pd.DataFrame` | Retorno de `download_eva_reciente()` (D1.2) |
| `output_dir` | `Path` | `DATA_DIR` de `shared/config.py` |

Ambos DataFrames ya tienen el esquema unificado. Esta tarea no hace limpieza adicional de columnas individuales — solo valida consistencia entre fuentes y ensambla.

---

## Outputs
| Archivo | Contenido |
|---------|-----------|
| `data/eva_historica.parquet` | `df_historica` sin modificaciones |
| `data/eva_reciente.parquet` | `df_reciente` sin modificaciones |
| `data/eva_completa.parquet` | Concatenación ordenada de ambos |

---

## Lógica de ensamble
```
1. Verificar que df_historica y df_reciente no estén vacíos
   Si alguno está vacío → loggear warning (no lanzar excepción)

2. Verificar que no haya solapamiento de años entre fuentes:
   - df_historica["año"].max() debe ser <= 2018
   - df_reciente["año"].min() debe ser >= 2019
   Si hay solapamiento → loggear warning con los años duplicados

3. Guardar df_historica → data/eva_historica.parquet

4. Guardar df_reciente → data/eva_reciente.parquet

5. Concatenar: df_completa = pd.concat([df_historica, df_reciente], ignore_index=True)

6. Ordenar por (codigo_dane, cultivo, año, periodo)

7. Verificar integridad: len(df_completa) == len(df_historica) + len(df_reciente)
   Si no coincide → lanzar AssertionError con detalle

8. Guardar df_completa → data/eva_completa.parquet

9. Imprimir resumen de ejecución (ver sección Logging)

10. Retornar df_completa
```

---

## Validaciones de consistencia entre fuentes
Estas validaciones son advertencias, no errores fatales (salvo la de integridad):

| Validación | Nivel | Acción |
|------------|-------|--------|
| Municipios en histórica ≠ municipios en reciente | WARNING | Loggear diferencia |
| Cultivos en histórica ≠ cultivos en reciente | WARNING | Loggear diferencia |
| Año máximo histórica > 2018 | WARNING | Loggear años problemáticos |
| Año mínimo reciente < 2019 | WARNING | Loggear años problemáticos |
| `len(completa) != len(hist) + len(rec)` | ERROR | Lanzar `AssertionError` |

---

## Logging y resumen de ejecución
El resumen debe imprimirse con `logging.info` al finalizar:

```
=== Resumen Pipeline EVA ===
EVA histórica:   1.847 registros | años 2007-2018 | 15/15 municipios
EVA reciente:      612 registros | años 2019-2024 | 15/15 municipios
EVA completa:    2.459 registros | años 2007-2024

Por cultivo (completa):
  Café:   1.102 registros | rendimiento medio: 1.14 t/ha
  Cacao:    687 registros | rendimiento medio: 0.72 t/ha
  Maíz:     670 registros | rendimiento medio: 2.31 t/ha

Municipios sin datos en alguna fuente: ninguno
Registros con rendimiento=None: 23 (0.9%)
Archivos guardados:
  data/eva_historica.parquet
  data/eva_reciente.parquet
  data/eva_completa.parquet
```

Para calcular `X/15 municipios`: contar `codigo_dane` únicos en cada DataFrame.
Para `rendimiento medio`: `df[df["cultivo"]==c]["rendimiento"].mean()` ignorando NaN.

---

## Interfaz de la función
```python
def run_pipeline(
    output_dir: Path = DATA_DIR,
    codigos_dane: list[str] | None = None,
) -> pd.DataFrame:
    """
    Ejecuta el pipeline completo EVA: descarga, limpia, unifica y guarda.

    Internamente llama a download_eva_historica() y download_eva_reciente(),
    luego ensambla y guarda los 3 archivos Parquet.

    Args:
        output_dir: Directorio donde guardar los Parquet. Default: DATA_DIR.
        codigos_dane: Municipios a procesar. Si None, usa MVP_CODIGOS.

    Returns:
        eva_completa como DataFrame ordenado.

    Raises:
        AssertionError: Si len(completa) != len(hist) + len(rec).
        requests.RequestException: Si alguna descarga falla tras reintentos.
    """

def load_eva_completa(data_dir: Path = DATA_DIR) -> pd.DataFrame:
    """
    Carga eva_completa.parquet desde disco.

    Raises:
        FileNotFoundError: Si el archivo no existe (ejecutar run_pipeline primero).
    """
```

---

## Criterios de aceptación
- [ ] Los 3 archivos Parquet existen en `data/` tras ejecutar `run_pipeline()`
- [ ] `len(eva_completa) == len(eva_historica) + len(eva_reciente)` exactamente
- [ ] `eva_historica` contiene solo años 2007–2018
- [ ] `eva_reciente` contiene solo años 2019–2024
- [ ] `eva_completa` está ordenada por `(codigo_dane, cultivo, año, periodo)`
- [ ] El resumen de ejecución se imprime con el formato especificado
- [ ] `load_eva_completa()` carga el archivo y retorna el mismo DataFrame
- [ ] `run_pipeline()` puede ejecutarse como script: `python -m modules.agricultural.ingestion`
