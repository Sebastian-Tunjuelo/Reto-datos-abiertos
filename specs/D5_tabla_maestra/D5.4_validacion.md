# D5.4 — Script de validación y criterios de aceptación

## Metadatos
| Campo | Valor |
|-------|-------|
| ID | D5.4 |
| Módulo | Script independiente (no forma parte del módulo productivo) |
| Depende de | D5.3 (`data/tabla_maestra.parquet`) |
| Produce para | — (validación final, no produce datos) |
| Estado | ✅ Listo |

---

## Agente

**Rol**: Eres un ingeniero de QA de datos. Tu trabajo es escribir un script de validación independiente que actúe como árbitro objetivo de la tabla maestra. No tienes acceso al código de producción — solo lees `tabla_maestra.parquet` y verificas que cumple el contrato de datos y las reglas de no fuga del modelo.

**Mentalidad**: Piensa como un auditor del pipeline de ML. Además de verificar el esquema y los tipos, debes confirmar que no hay fuga de información temporal: ninguna feature del mismo año que la variable objetivo puede estar presente. Un dataset de entrenamiento con fuga produce modelos que parecen excelentes en validación pero fallan en producción. Cada aserción debe ser independiente y tener un mensaje de error que explique exactamente qué falló y por qué importa.

**Restricciones**:
- No importar ni llamar funciones del módulo `modules/predictive/` — solo leer el Parquet
- No modificar `tabla_maestra.parquet` bajo ninguna circunstancia
- Cada aserción debe tener un mensaje de error con el prefijo `❌` y descripción del problema
- Si una aserción falla, el script debe detenerse inmediatamente con `AssertionError`
- El script debe poder ejecutarse de forma aislada sin depender del estado del proceso anterior

---

## Objetivo
Verificar que `tabla_maestra.parquet` cumple todos los criterios de aceptación del pipeline D5: esquema correcto, tipos correctos, cobertura del MVP, integridad de los joins, ausencia de fuga de información y split temporal correcto.

---

## Cuándo ejecutar
```bash
# Después de correr el pipeline:
python -m modules.predictive.feature_builder

# Luego validar:
python specs/D5_tabla_maestra/validate_d5.py
```

O desde Python:
```python
from specs.D5_tabla_maestra.validate_d5 import run_validations
run_validations()  # Lanza AssertionError si algo falla
```

---

## Checklist de criterios de aceptación

### Estructura del archivo
- [ ] `data/tabla_maestra.parquet` existe

### Esquema y tipos
- [ ] Las 28 columnas del esquema final están presentes en el orden correcto
- [ ] `codigo_dane` es `object` (string) con longitud exacta de 5 en todos los registros
- [ ] `año` es `int64` o `Int64`; valores en rango 2007–2024
- [ ] `rendimiento` es `float64`
- [ ] Todas las features numéricas son `float64`
- [ ] `señal_riesgo_eco` contiene solo `{'Bajo', 'Medio', 'Alto', NaN}`

### Ausencia de fuga de información
- [ ] Las columnas `area_cosechada` y `produccion` **no están** en la tabla
- [ ] La columna `rendimiento` (variable objetivo) está presente pero **no** como feature rezagada del mismo año
- [ ] `rendimiento_t1` para el año `t` es igual a `rendimiento` del año `t-1` del mismo municipio/cultivo

### Cobertura del MVP
- [ ] Los 15 `MVP_CODIGOS` están presentes en `codigo_dane`
- [ ] `cultivo` contiene exactamente `{'Café', 'Cacao', 'Maíz'}`
- [ ] Hay al menos una fila por cada combinación `(codigo_dane, cultivo)` — 45 combinaciones mínimo

### Integridad de los joins
- [ ] No hay duplicados por `(codigo_dane, cultivo, año)`
- [ ] El número de filas es igual al número de filas en `eva_completa` agregada a nivel anual
  (verificar leyendo `eva_completa.parquet` y comparando)
- [ ] El número de combinaciones `(codigo_dane, cultivo)` coincide con las combinaciones presentes
  en `eva_completa.parquet`

### Features rezagadas
- [ ] `rendimiento_t1` es NaN para el primer año de cada grupo `(codigo_dane, cultivo)`
- [ ] `rendimiento_prom3a` es NaN cuando hay menos de 1 año de historial
- [ ] `area_cambio_pct` es NaN cuando `area_sembrada_t1` es NaN

### Split temporal
- [ ] Hay filas con `año <= 2021` (conjunto de entrenamiento)
- [ ] Hay filas con `año == 2022` (conjunto de validación)
- [ ] Hay filas con `año >= 2023` (conjunto de test — no tocar)
- [ ] El split train/val/test no tiene solapamiento de años

---

## Script de validación
Guardar como `specs/D5_tabla_maestra/validate_d5.py`:

```python
"""
Script de validación para el pipeline D5 — Tabla Maestra.
Ejecutar después de modules.predictive.feature_builder.build_tabla_maestra()
"""
import sys
from pathlib import Path

import numpy as np
import pandas as pd

ROOT = Path(__file__).parent.parent.parent
sys.path.insert(0, str(ROOT))

from shared.config import DATA_DIR, CULTIVOS_MVP, TRAIN_HASTA, VAL_AÑO, TEST_DESDE
from shared.dane_codes import MVP_CODIGOS

COLUMNAS_ESPERADAS = [
    'codigo_dane', 'municipio', 'departamento', 'cultivo', 'año',
    'rendimiento', 'area_sembrada',
    'rendimiento_t1', 'rendimiento_t2', 'rendimiento_t3',
    'rendimiento_prom3a', 'rendimiento_tend3a',
    'area_sembrada_t1', 'area_cambio_pct',
    'prec_acum_mm', 'temp_media_c', 'hum_media_pct', 'dias_secos',
    'anomalia_prec', 'anomalia_temp',
    'pct_aptitud_alta', 'pct_aptitud_media', 'pct_aptitud_baja', 'pct_exclusion',
    'pct_condicionada',
    'fertilizantes', 'pct_fertilizantes', 'señal_riesgo_eco',
]

COLUMNAS_FUGA = ['area_cosechada', 'produccion']
CULTIVOS_ESPERADOS = set(CULTIVOS_MVP)
SEÑALES_VALIDAS = {'Bajo', 'Medio', 'Alto'}


def run_validations(data_dir: Path = DATA_DIR) -> None:
    """Ejecuta todas las validaciones. Lanza AssertionError si alguna falla."""

    # --- Cargar archivo ---
    path = data_dir / 'tabla_maestra.parquet'
    assert path.exists(), f"❌ Archivo no encontrado: {path}"
    df = pd.read_parquet(path)

    # --- Esquema ---
    faltantes = [c for c in COLUMNAS_ESPERADAS if c not in df.columns]
    assert not faltantes, f"❌ Columnas faltantes: {faltantes}"

    extras = [c for c in df.columns if c not in COLUMNAS_ESPERADAS]
    assert not extras, f"❌ Columnas inesperadas (posible fuga): {extras}"

    assert list(df.columns) == COLUMNAS_ESPERADAS, (
        "❌ El orden de columnas no coincide con el esquema esperado"
    )

    # --- Tipos ---
    assert df['codigo_dane'].str.len().eq(5).all(), (
        "❌ codigo_dane con longitud != 5"
    )
    assert df['año'].dtype in ('int64', 'Int64'), (
        f"❌ año no es int64/Int64: {df['año'].dtype}"
    )
    assert df['rendimiento'].dtype == 'float64', (
        f"❌ rendimiento no es float64: {df['rendimiento'].dtype}"
    )
    for col in ['rendimiento_t1', 'rendimiento_prom3a', 'prec_acum_mm',
                'pct_aptitud_alta', 'fertilizantes', 'pct_fertilizantes']:
        assert df[col].dtype == 'float64', f"❌ {col} no es float64: {df[col].dtype}"

    señales_reales = set(df['señal_riesgo_eco'].dropna().unique())
    invalidas = señales_reales - SEÑALES_VALIDAS
    assert not invalidas, f"❌ señal_riesgo_eco con valores inválidos: {invalidas}"

    # --- Ausencia de fuga ---
    for col in COLUMNAS_FUGA:
        assert col not in df.columns, (
            f"❌ Columna de fuga detectada: '{col}' no debe estar en la tabla maestra"
        )

    # --- Verificar rendimiento_t1 es lag correcto ---
    sample = df[df['cultivo'] == CULTIVOS_MVP[0]].sort_values(['codigo_dane', 'año'])
    for codigo in sample['codigo_dane'].unique()[:3]:  # verificar 3 municipios
        sub = sample[sample['codigo_dane'] == codigo].reset_index(drop=True)
        for i in range(1, len(sub)):
            rend_actual_t1 = sub.loc[i, 'rendimiento_t1']
            rend_año_anterior = sub.loc[i - 1, 'rendimiento']
            if pd.notna(rend_actual_t1) and pd.notna(rend_año_anterior):
                assert abs(rend_actual_t1 - rend_año_anterior) < 1e-6, (
                    f"❌ rendimiento_t1 incorrecto para {codigo} año {sub.loc[i, 'año']}: "
                    f"esperado {rend_año_anterior:.4f}, obtenido {rend_actual_t1:.4f}"
                )

    # --- Cobertura del MVP ---
    codigos_presentes = set(df['codigo_dane'].unique())
    faltantes_mvp = set(MVP_CODIGOS) - codigos_presentes
    assert not faltantes_mvp, f"❌ Municipios MVP sin datos: {faltantes_mvp}"

    cultivos_reales = set(df['cultivo'].unique())
    assert cultivos_reales == CULTIVOS_ESPERADOS, (
        f"❌ Cultivos inesperados: {cultivos_reales - CULTIVOS_ESPERADOS}"
    )

    combinaciones = df.groupby(['codigo_dane', 'cultivo']).ngroups
    assert combinaciones >= 45, (
        f"❌ Se esperaban >= 45 combinaciones municipio/cultivo, hay {combinaciones}"
    )

    # --- Integridad de joins ---
    llave = ['codigo_dane', 'cultivo', 'año']
    dups = df.duplicated(llave).sum()
    assert dups == 0, f"❌ {dups} duplicados por {llave}"

    eva_path = data_dir / 'eva_completa.parquet'
    if eva_path.exists():
        eva = pd.read_parquet(eva_path)
        # Agregar EVA a nivel anual para comparar
        n_eva_anual = eva.groupby(['codigo_dane', 'cultivo', 'año']).ngroups
        assert len(df) == n_eva_anual, (
            f"❌ Tabla maestra ({len(df)} filas) != EVA agregada anual ({n_eva_anual} filas)"
        )

    # --- Features rezagadas en primer año ---
    primer_año_por_grupo = df.groupby(['codigo_dane', 'cultivo'])['año'].min()
    for (codigo, cultivo), año_min in primer_año_por_grupo.items():
        fila = df[(df['codigo_dane'] == codigo) & (df['cultivo'] == cultivo) & (df['año'] == año_min)]
        assert fila['rendimiento_t1'].isna().all(), (
            f"❌ rendimiento_t1 no es NaN para el primer año de {codigo}/{cultivo} ({año_min})"
        )

    # --- Split temporal ---
    assert (df['año'] <= TRAIN_HASTA).any(), f"❌ No hay filas de entrenamiento (año <= {TRAIN_HASTA})"
    assert (df['año'] == VAL_AÑO).any(), f"❌ No hay filas de validación (año == {VAL_AÑO})"
    assert (df['año'] >= TEST_DESDE).any(), f"❌ No hay filas de test (año >= {TEST_DESDE})"

    # --- Resumen si todo pasó ---
    n_train = (df['año'] <= TRAIN_HASTA).sum()
    n_val = (df['año'] == VAL_AÑO).sum()
    n_test = (df['año'] >= TEST_DESDE).sum()

    print("✅ Todos los criterios de aceptación pasaron")
    print(f"\nResumen:")
    print(f"  Total filas:    {len(df):>6,}")
    print(f"  Municipios:     {df['codigo_dane'].nunique():>6}/15")
    print(f"  Cultivos:       {df['cultivo'].nunique():>6}/3")
    print(f"  Años:           {df['año'].min()}–{df['año'].max()}")
    print(f"\n  Split temporal:")
    print(f"    Train  (≤{TRAIN_HASTA}): {n_train:>5,} filas")
    print(f"    Val    ({VAL_AÑO}):   {n_val:>5,} filas")
    print(f"    Test   (≥{TEST_DESDE}):   {n_test:>5,} filas")
    print(f"\n  Cobertura de features (% filas con valor):")
    for col in ['rendimiento_t1', 'rendimiento_prom3a', 'prec_acum_mm',
                'pct_aptitud_alta', 'fertilizantes']:
        pct = df[col].notna().mean() * 100
        print(f"    {col:<25}: {pct:>5.1f}%")
    print(f"\n  Columnas de fuga verificadas: ninguna detectada ✅")


if __name__ == "__main__":
    run_validations()
```

---

## Criterios de aceptación de este script
- [x] El script corre sin errores cuando D5.3 se completó correctamente
- [x] Cada aserción tiene un mensaje de error descriptivo con prefijo `❌`
- [x] Se verifica explícitamente la ausencia de columnas de fuga (`area_cosechada`, `produccion`)
- [x] Se verifica que `rendimiento_t1` es el lag correcto en al menos 3 municipios
- [x] El resumen final muestra el split temporal y la cobertura de features clave
- [x] El script puede importarse como módulo (`from specs... import run_validations`)
- [x] El script puede ejecutarse directamente: `python specs/D5_tabla_maestra/validate_d5.py`

### Nota sobre ejecución en modo muestreo IDEAM
Si se usa muestreo (`IDEAM_ESTACIONES_POR_MUN` o `IDEAM_MAX_ESTACIONES`) para acelerar el pipeline climático,
es posible que fallen criterios de cobertura (ej. combinaciones municipio/cultivo) debido a NaN extensivos.
Usar solo para pruebas de flujo extremo a extremo, no para validación final.
